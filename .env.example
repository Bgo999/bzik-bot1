### Example environment variables (DO NOT commit real keys)
# Provide these values in your Render service settings (or locally for development).

# Comma-separated OpenRouter API keys (server-side only)
OPENROUTER_API_KEYS=sk-or-v1-xxxxx,sk-or-v1-yyyyy

# Optional: frontend build-time override for chat endpoint
# Example: https://bzik-api.onrender.com/chat
VITE_CHAT_ENDPOINT=
